---
"@xubylele/schema-forge-core": minor
---

✨ feat(policies): add `all` command and `to` roles support

- Add `all` command to apply a policy to SELECT, INSERT, UPDATE, and DELETE.
- Support `to` clause for specifying roles in policies.
- Update parsing, validation, and SQL generation to handle new policy features.
- Add tests covering new policy commands and role assignments.
